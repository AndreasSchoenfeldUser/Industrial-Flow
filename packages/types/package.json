{
  "name": "@industrialflow/types",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "tsc --noEmit",
    "lint": "echo \"(types) no lint configured\" && exit 0",
    "test": "echo \"(types) no tests\" && exit 0"
  },
  "devDependencies": {
    "typescript": "5.6.3"
  }
}
